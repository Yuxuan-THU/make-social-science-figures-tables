# Independent forward-test prompts

1. Use only `$make-social-science-figures-tables` and the supplied coefficient CSV to create an AJPS coefficient plot. Deliver R, PDF, SVG, PNG, redraw-data CSV, and QA.
2. Use only `$make-social-science-figures-tables` and the supplied event-study CSV to create an APSR event-study figure with a clearly marked omitted period. Deliver the five-part bundle.
3. Use only `$make-social-science-figures-tables` and the supplied synthetic regression CSV to fit the explicitly requested `outcome ~ treatment + age + urban` model and create both APSR and AJPS LaTeX regression tables. Compile and inspect both.
